export const SET_GAS = (state, gas) => {
  state.gas = gas
}

export const SET_VEHICLE = (state, vehicle) => {
  state.vehicle = vehicle
}

export const SET_ROUTE = (state, route) => {
  state.route = route
}

export const SET_SRC = (state, src) => {
  state.src = src
}

export const SET_GAS_PRICE = (state, gasPrice) => {
  state.gasPrice = gasPrice
}

export const SET_PASSENGERS = (state, passengers) => {
  state.passengers = passengers
}

export const SET_FINAL_VAL = (state, finalCalc) => {
  state.finalCalc = finalCalc
}
